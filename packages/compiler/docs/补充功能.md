

1. 组件支持对属性赋值在当前位置中赋值为 模板片段

   ```pug
   ${Child} header=
   	div text="header头"
   | 
   	div	text="children"
   ```

2. 字符内自动转义

   ```pug
   div text={'123\n'}
   ```

   自动转义为

   ```pug
   div text={'123\\n'}
   ```

3. 静态复制支持计算

   ```pug
   ${Child} value=${(data) => `${data.value}元`}
   ```

4. Ref

   ```pug
   div ref={xxx}
   ```

5. Context

   ```pug
   ctx xxxStore={a}
   ```

6. 自定义 Effect，或者 Noop Effect